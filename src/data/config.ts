export const siteConfig = {
  /**
   * Set to true when actively job-seeking.
   * Controls the "Looking for" section on /now and any hiring signals site-wide.
   * Flip this off once landed — removes all job-search language from the public site.
   */
  activelyLooking: true,
} as const;
