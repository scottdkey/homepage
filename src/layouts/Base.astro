---
import { ClientRouter } from 'astro:transitions';
interface Props {
  title: string;
  description?: string;
}
const { title, description = 'Scott Key — Senior Software Engineer' } = Astro.props;
---
<!doctype html>
<html lang="en" data-theme="tide">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="robots" content="noai, noimageai" />
    <title>{title} — Scott Key</title>
    <ClientRouter />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <!-- Prevent theme flash on initial load -->
    <script is:inline>
      (function () {
        const html = document.documentElement;
        html.setAttribute('data-theme', localStorage.getItem('theme') || 'tide');
        html.setAttribute('data-mode', localStorage.getItem('colorMode') || 'dark');
        if (localStorage.getItem('readingMode') === 'true') {
          html.setAttribute('data-reading-mode', 'true');
        }
      })();
    </script>
    <!-- Preserve theme across client-side navigations -->
    <script>
      document.addEventListener('astro:before-swap', (e: any) => {
        const html = document.documentElement;
        const incoming = e.newDocument.documentElement;
        incoming.setAttribute('data-theme', html.getAttribute('data-theme') || 'tide');
        incoming.setAttribute('data-mode', html.getAttribute('data-mode') || 'dark');
        const rm = html.getAttribute('data-reading-mode');
        if (rm) incoming.setAttribute('data-reading-mode', rm);
        else incoming.removeAttribute('data-reading-mode');
      });
    </script>
  </head>
  <body>
    <a href="#main" class="skip-link">Skip to content</a>
    <slot name="header" />
    <main id="main" tabindex="-1">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
